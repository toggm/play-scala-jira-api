# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# oauth services
GET		  /oauth								 controllers.OAuthController.index
POST      /oauth/requestToken                    controllers.OAuthController.obtainRequestToken 
POST      /oauth/accessToken                     controllers.OAuthController.obtainAccessToken
